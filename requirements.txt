aiogram>=3.10.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.1
pydantic>=2.8.0

# бесплатный провайдер по умолчанию
groq>=0.11.0

# опционально — нужен только если LLM_PROVIDER=gemini
google-generativeai>=0.8.0

# опционально — нужен только если LLM_PROVIDER=claude (платный)
anthropic>=0.40.0
